# Unpinned versions - let pip choose latest compatible versions with Python 3.13 wheels
# This eliminates build errors by automatically selecting versions with pre-built wheels

# Core web framework
fastapi
pydantic

# ASGI Server - must be installed with gunicorn to enable UvicornWorker
# Using gunicorn[uvicorn] installs both gunicorn AND uvicorn with proper integration
uvicorn
gunicorn[uvicorn]

# Machine learning libraries
numpy
pandas
scikit-learn
scipy
joblib
shap
dill

# Utilities
python-multipart
